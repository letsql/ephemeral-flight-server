[project]
name = "flight-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.1.3",
    "pandas>=2.2.3",
    "pyarrow>=18.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.9.0",
]
